# Create a folder locally
mkdir myfoldername

# Add a file to the folder (GitHub requires at least one file per folder)
touch myfoldername/.gitkeep

# Or add an actual file
echo "# My Folder" > myfoldername/README.md

# Add, commit, and push
git add myfoldername/
git commit -m "Create myfoldername folder"
git push
